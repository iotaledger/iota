# Type Alias: IotaSystemStateSummary

> **IotaSystemStateSummary**: `object` \| `object`

This is the JSON-RPC type for IOTA system state objects. It is an enum type that can represent
either V1 or V2 system state objects.
