---
description: Learn how to set up an IOTA full node from source.
title: Build From Source
tags: [node-operation]
teams:
  - iotaledger/node
---
import Quiz from '@site/src/components/Quiz';
import questions from '/json/node-operators/iota-full-node/node-setup.json';
import WarningAdvanced from './../../_snippets/warning-advanced-instructions-node-setup.mdx'
import FullnodeRequirements from '../../_snippets/operator/fullnode-requirements-tab.mdx'

import {Networks} from '@site/src/components/constant'
import Ports from './../../_snippets/operator/ports.mdx'
import Genesis from './../../_snippets/operator/genesis.mdx'

<WarningAdvanced/>

<Ports/>

## 0. Hardware requirements

<FullnodeRequirements/ >

## 1. Software requirements

- Linux (recommended):
  - For this installer to work, use a Linux distribution with `systemd` and `apt-get`.
  - IOTA recommends and supports Ubuntu or Debian.
  - Building from source also works on macOS (but systemd-related steps won't).
- Rust/cargo stable >1.85 . If needed, [update Rust](https://www.rust-lang.org/tools/install).


## 2. Run the installer script

In a new directory, (e.g. `mkdir -p $HOME/iota && cd $HOME/iota`), run:

#### TODO in 6017 change branch last before merging ??
```shell
bash -i <(curl -sSf https://raw.githubusercontent.com/iotaledger/iota/refs/heads/docs/further-node-operator-docs-improvements/setups/fullnode/systemd/setup.sh)
```

#### Installer script options (via environment variables)
You can override these values when running the script (optional):
#### TODO in 6017 re-check before PR that these var names are still correct
| Env variable | Description | Values | Default Value |
| --- | --- | --- | --- |
| NETWORK | The IOTA public network you want your node to connect to | "testnet", "mainnet" | "testnet" |
| CLONE_DIR | Directory to clone a fresh copy of the iota repo into | Path to directory | `$HOME/.cache/iota-clone` |
| NODE_WORKDIR | Directory for all node data (config, binary, DB) | Path to directory | `/opt/iota` |
| CONFIG_DIR | Directory for storing iota-node config files | Path to directory | `/opt/iota/config` |
| BIN_DIR | Directory for the iota-node binary | Path to directory | `/opt/iota/bin` |

To pass env vars to the installer, declare them as follows, in front of the call to the installer:
```shell
NETWORK="testnet" CLONE_DIR="$HOME/.cache/iota"  bash -i <(curl https://raw.githubusercontent.com/iotaledger/iota.js/master/installer.sh)
```

The installer will perform the following steps:
1. Check your rust toolchain version
2. Install system packages (libraries & other dependencies):  
  `tzdata`, `libprotobuf-dev`, `ca-certificates`, `build-essential`, `libssl-dev`, `libclang-dev`, `libpq-dev`, `pkg-config`, `openssl`, `protobuf-compiler`, `git`, `clang`, `cmake`
3. Clone the [iota repo](https://github.com/iotaledger/iota), set to the right branch for the [network](../../developer/getting-started/connect.mdx#iota-networks) you chose
4. Build the iota-node binary  
  The built binary is located at `/opt/iota/bin/iota-node` (or in `$BIN_DIR` if specified)
5. Create a user called iota, make it own directories for service binary, config, data
6. Create a node config file, download genesis/migration blobs  
  The config and blob files are located at `/opt/iota/config` (or in `$CONFIG_DIR` if specified)
6. Create a systemd service unit file  
  The service definition file is located at `/etc/systemd/system/iota-node.service`
7. Start the service



## 3. Check your installation

Your IOTA node should now be running and connected to the network you chose.

Check that the iota-node service is active and running:
```shell
systemctl status iota-node
```

Check (and follow) the logs:
```shell
journalctl -u iota-node -f
```

If you need to stop the service, you can run `systemctl stop iota-node`


### Check your node configuration

The configuration file is at `/opt/iota/config/fullnode.yaml` (or inside `$CONFIG_DIR` if set)

The p2p section should include known public peer nodes, like this:




<Tabs groupId="network" queryString>
  <TabItem label="Testnet" value="testnet">
    ```yaml
    p2p-config:
      seed-peers:
        - address: /dns/access-0.r.testnet.iota.cafe/udp/8084
          peer-id: 46064108d0b689ed89d1f44153e532bb101ce8f8ca3a3d01ab991d4dea122cfc
        - address: /dns/access-1.r.testnet.iota.cafe/udp/8084
          peer-id: 8ffd25fa4e86c30c3f8da7092695e8a103462d7a213b815d77d6da7f0a2a52f5
    ```
  </TabItem>

{/* 
  ### TODO in 6017 add values for mainnet
  <TabItem label="Mainnet" value="mainnet">
    ```yaml
    p2p-config:
      seed-peers:
        - address: /dns/access-0.r.devnet.iota.cafe/udp/8084
          peer-id: 01589ac910a5993f80fbc34a6e0c8b2041ddc5526a951c838df3037e11ab0188
        - address: /dns/access-1.r.devnet.iota.cafe/udp/8084
          peer-id: 32875c547ea3b44fa08a711646cedb70fa0c97959d236578131505da09723add
    ```
  </TabItem>
*/}
</Tabs>



The archival section of the config file should look like this:

<Tabs groupId="network" queryString>
  <TabItem label="Mainnet" value="mainnet">
    ```yaml
    state-archive-read-config:
      - object-store-config:
          object-store: "S3"
          aws-endpoint: "https://archive.mainnet.iota.cafe"
          aws-virtual-hosted-style-request: true
          object-store-connection-limit: 20
          no-sign-request: true
        concurrency: 5
        use-for-pruning-watermark: false
    ```
  </TabItem>

  <TabItem label="Testnet" value="testnet">
    ```yaml
    state-archive-read-config:
      - object-store-config:
          object-store: "S3"
          aws-endpoint: "https://archive.testnet.iota.cafe"
          aws-virtual-hosted-style-request: true
          object-store-connection-limit: 20
          no-sign-request: true
        concurrency: 5
        use-for-pruning-watermark: false
    ```
  </TabItem>
</Tabs>

This allows nodes that fall behind to catch up by downloading archive data rather than relying on synchronization. See [IOTA Archives](configs/archives) for details.

### Reach your node via the RPC endpoint

Your full node serves the read endpoints of the IOTA JSON-RPC API at: <code>{Networks.iota_localnet.jsonRpcUrl}</code>.

Register a new iota client environment with your node's RPC endpoint
```shell
iota client new-env --alias=ALIAS --rpc http://127.0.0.1:9000
iota client switch --env ALIAS
```
Then query the chain identifier from the RPC endpoint:
```shell
iota client chain-identifier
```



## 4. Customize your node's configuration

#### TODO in 6017 links to pruning, archives, networking, snapshots

### 4.2 Networking

To synchronize the state, the node needs to establish connections with other nodes that have already joined the network. Ensure that port 8084 is open and set up an external address for node discovery.

The full node uses the following ports by default:

| Protocol/Port | Reachability     | Purpose                           | Expose port ? |
| ------------- | ---------------- | --------------------------------- | --------- |
| UDP/8084      | Inbound/Outbound | Peer-to-peer state sync interface | Needs to be reachable from the internet |
| TCP/9000      | Outbound         | JSON RPC Address / Web socket     | (optional. Can be private for your use only) |
| TCP/9184      | Localhost        | Metrics scraping for your monitoring                  | (optional) |



### 4.2 Storage and pruning (optional)

Choose how much data and how long your node keeps it by setting custom [pruning settings](https://docs.iota.org/operator/full-node/configs/pruning).


### 4.3 Data storage paths (optional)

You can adapt the node's configuration to your needs by changing the `fullnode.yaml` file (at `/opt/iota/config/fullnode.yaml`, or, if set, inside `$CONFIG_DIR`).  
Don't forget to restart the `iota-node` systemd service after making changes to the configuration.

To set paths for data files, change these values in `fullnode.yaml`:

```yaml
# Set a custom path for storing the node's database
db-path: "/db-files/iota-fullnode"

# Set a custom path to the genesis blob
genesis:
  genesis-file-location: "/iota-fullnode/genesis.blob"

# Set a custom path for the migration blob
migration-tx-data-path: "/opt/iota/config/migration.blob"
```













## Troubleshooting

### `cannot find -lpq`

If, during the compilation step, you receive a `cannot find -lpq` error, you are missing the `libpq` library. 
Use `sudo apt-get install libpq-dev` to install on Linux, or `brew install libpq` on MacOS. 
After you install on MacOS, create a Homebrew link using `brew link --force libpq`. 
For further context, reference the [issue on Stack Overflow](https://stackoverflow.com/questions/70313347/ld-library-not-found-for-lpq-when-build-rust-in-macos?rq=1).

### Listener: Address Already In Use

If you receive the following error:

```shell
panicked at error binding to 0.0.0.0:9184: error creating server listener: Address already in use (os error 98)
```

Update the metrics address in your fullnode.yaml file to use port `9180`.

```yaml
metrics-address: "0.0.0.0:9180"
```

## Update Your Full node

Whenever IOTA releases a new version, you must update your full node with the release to ensure compatibility with the network it connects to. For example, if you use IOTA Testnet you should install the version of IOTA running on IOTA Testnet.

Use the following steps to update your full node:

1. Shut down your running full node.
2. Move into the directory for your local `iota` repository and remove `genesis.blob` and `migration.blob` files:

    ```shell
    rm genesis.blob migration.blob
    ```

3. Fetch the latest changes by pulling from the [IOTA repository](https://github.com/iotaledger/iota):
    
    ```shell
    git pull
    ```

4. Download the latest genesis and migration blob:
    <Genesis/>

5. (Optional) Update your `fullnode.yaml` configuration file, if needed.  

    ```bash
    cp setups/fullnode/fullnode-template.yaml fullnode.yaml
    ```

6. Recompile your IOTA full node:

    ```bash
    cargo build --release --bin iota-node
    ```

7. Restart your IOTA full node:

    ```shell
    ./target/release/iota-node --config-path fullnode.yaml
    ```

Your full node restarts on: <code>{Networks.iota_localnet.jsonRpcUrl}</code>.

:::info

If your full node is configured to prune transactions, Archival nodes can help ensure lagging peer nodes don't lose any information. For more information, see [IOTA Archives](../common/archives.mdx).

:::
<Quiz questions={questions} />
