---
tags: [ts-sdk]
teams:
   - iotaledger/tooling
---
# Themes

You can provide a theme to the `WalletProvider` component to customize the components in dApp Kit.

```tsx file=<rootDir>/docs/examples/typescript/src/ts-sdk/dapp-kit/theme-basic.tsx
```

## Dynamic themes

To dynamically change the theme, you can provide multiple themes to the `WalletProvider` component:

```tsx file=<rootDir>/docs/examples/typescript/src/ts-sdk/dapp-kit/theme-dynamic.tsx
```

## Theme variables

To define a custom theme, implement the `ThemeVars` interface with CSS variable values for your
theme:

```tsx file=<rootDir>/docs/examples/typescript/src/ts-sdk/dapp-kit/theme-custom-variables.tsx
```
