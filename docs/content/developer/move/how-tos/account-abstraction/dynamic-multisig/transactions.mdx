---
title: Multisig Transactions
description: "How to manage transaction proposals and approvals in a dynamic multisig account"
tags:
  - how-to
  - move-sc
  - move-vm
---

import AccountAbstraction from '../../../../../_snippets/developer/move/aa.mdx';

<AccountAbstraction /> 

This how-to demonstrates how to manage transaction proposals and approvals in a dynamic multisig account.

## Example Code

1. Define the `Transaction` and `Transactions` data structures.
```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/transactions.move#L20-L35
```
2. Create the transactions store (called once during multisig account creation).

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/transactions.move#L36-L42
```

3. Check whether a transaction proposal exists and access proposal data.

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/transactions.move#L49-L59
```

4. Add a new transaction proposal (the proposer is automatically the first approver).

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/transactions.move#L60-L68
```

5. Approve an existing transaction (prevents duplicate approvals from the same member).

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/transactions.move#L94-L100
```

## Expected Behavior

Transaction proposals and approvals are stored on-chain until execution or removal. The multisig authenticator relies on this data to decide whether a transaction is authorized.

## Full Example Code

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/transactions.move
```
