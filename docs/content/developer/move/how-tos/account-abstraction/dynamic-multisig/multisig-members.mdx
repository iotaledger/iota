---
title: Multisig Members
description: "How to define and manage multisig members in a dynamic multisig account"
tags:
  - how-to
  - move-sc
  - move-vm
---

import AccountAbstraction from '../../../../../_snippets/developer/move/aa.mdx';

<AccountAbstraction /> 

This how-to demonstrates how to define and manage multisig members in a dynamic multisig account.
If you want to learn more about the concepts of Account Abstraction, check out the [Account Abstraction Introduction](../../../explanations/account-abstraction.mdx).

## Example Code

1. Define the `Member` and `Members` data structures.
```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/members.move#L19-L32
```
2. Create a members list with validation (no duplicates, matching address/weight lengths).

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/members.move#L33-L47
```

3. Mutate member state for trusted modules (weight changes, configuration rotation).

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/members.move#L48-L55
```

4. Check whether an address is a member and access member data.

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/members.move#L58-L72
```

5. Query member addresses, weights, and total voting power.

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/members.move#L73-L93
```

6. Validate multisig member inputs (prevent mismatched inputs and duplicate members).

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/members.move#L106-L118
```

## Expected Behavior

Only valid multisig member configurations can be created. All approval and authorization logic built on top of this module operates on a consistent, validated member set.

## Full Example Code

```move file=<rootDir>/examples/move/abstract_iota_accounts/dynamic_multisig_account/sources/members.move
```
