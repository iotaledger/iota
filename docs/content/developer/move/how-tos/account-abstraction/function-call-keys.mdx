---
title: Function Call Keys
description: "How to attach function-call-key based delegation to an IOTAccount in Move"
tags:
  - how-to
  - move-sc
  - move-vm
---

import AccountAbstraction from '../../../../_snippets/developer/move/aa.mdx';

<AccountAbstraction /> 

This how-to demonstrates how to attach function-call-key based delegation to an IOTAccount, allowing the owner to delegate specific Move function calls to other public keys.
If you want to learn more about the concepts of Account Abstraction, check out the [Account Abstraction Introduction](../../explanations/account-abstraction.mdx).

:::tip Recommended reading

This guide builds on patterns from the [IOTAccount (builder & core ops)](create-iotaccount.mdx) and [Public Key Authentication](create-public-key-authentication.mdx) how-tos. Reviewing those first will help you understand the account creation and public key verification used here.

:::

## Example Code

1. Create an IOTAccount with an owner public key and an attached function-call-keys store.

```move file=<rootDir>/examples/move/abstract_iota_accounts/function_call_keys/sources/function_call_keys.move#L73-L102
```

2. Grant, revoke, or check a function permission for a delegated public key (owner-only).

```move file=<rootDir>/examples/move/abstract_iota_accounts/function_call_keys/sources/function_call_keys.move#L166-L222
```

3. Define the core authenticator with dual flow: owner bypass (full power) or delegated key (single pre-approved function call per transaction).

```move file=<rootDir>/examples/move/abstract_iota_accounts/function_call_keys/sources/function_call_keys.move#L111-L164
```

4. Define the FunctionCallKeysStore with FunctionRef type, allow/disallow operations, and PTB extractor.

```move file=<rootDir>/examples/move/abstract_iota_accounts/function_call_keys/sources/function_call_keys_store.move#L40-L127
```

## Expected Behavior
- The account owner can execute any programmable transaction without restriction, while delegated keys are limited to one pre-approved Move function call per transaction.
- Transactions signed by delegated keys succeed only when the called (package, module, function) exists in the on-chain allow-set; otherwise, they are rejected during authentication.


## Full Example Code

The complete Move package, including comments and setup logic, is available here:

<Tabs groupId="module" queryString>
<TabItem value="function_call_keys" label="Function call">

```move file=<rootDir>/examples/move/abstract_iota_accounts/function_call_keys/sources/function_call_keys.move
```

</TabItem>
<TabItem value="store" label="Function call key store module">

```move file=<rootDir>/examples/move/abstract_iota_accounts/function_call_keys/sources/function_call_keys_store.move
```

</TabItem>
</Tabs>
