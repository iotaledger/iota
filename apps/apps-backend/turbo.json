{
    "extends": ["//"],
    "tasks": {
        "build": {
			"cache": false,
            "dependsOn": ["^build"],
            "outputs": [
				"dist/**"
			]
        },
		"vercel-build": {
			"cache": false,
            "dependsOn": ["^build"],
            "outputs": [
				"dist/**"
			]
        }
    }
}
