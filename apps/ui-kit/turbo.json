{
    "extends": ["//"],
    "tasks": {
        "build": {
            "outputs": ["dist/**"]
        },
        "build-storybook": {
            "dependsOn": ["^build"],
            "outputs": ["storybook-static/**"]
        }
    }
}
