To update fixtures in `tests/fixtures`, follow these steps:

1. Modify the `SELECTED_EPOCHS` constant in `src/bin/update_fixtures.rs`.
2. Run `cargo run --bin update-fixtures`.
3. If necessary, delete now excluded checkpoints.
